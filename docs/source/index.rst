Content Collection
##################

文档撰写规则:

1. 目录结构顺序: ``英文字母序小`` → ``英文字母序大`` → ``中文拼音序小`` → ``中文拼音序大``
2. 目录字符顺序: ``=`` → ``-`` → ````` → ``:`` → ``.`` → ``'`` → ``"`` → ``~`` → ``^`` → ``~`` → ``_`` → ``*`` → ``+``

.. toctree::
   :maxdepth: 2
   :hidden:

   note
   life
   system-install

.. toctree::
   :glob:
   :maxdepth: 2
   :hidden:
   :caption: 速查

   quick_search/*

.. toctree::
   :glob:
   :maxdepth: 2
   :hidden:
   :caption: 知识

   knowledge/*
